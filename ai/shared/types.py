from typing import Dict, Any

# a = str
# b = Dict[str, str]
# c = Dict[str, b]
# d = Dict[str, c]
# un = Union[a, b, c, d]

param_type = Dict[str, Any]  # TODO: Any => How to do recursive dictionary?
